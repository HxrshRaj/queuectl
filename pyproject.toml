[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "queuectl"
version = "0.0.0"
description = "QueueCTL"
requires-python = ">=3.11"

[project.scripts]
queuectl = "queuectl.main:main"